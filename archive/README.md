# Archive

Laravel backend was ported to Next.js API Routes in `frontend/`.

When `php artisan serve` is stopped, you can move the old backend:

```powershell
Move-Item backend archive\backend
```

Until then, `backend/` remains for reference; it is no longer required to run the app.
